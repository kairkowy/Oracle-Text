create table clob_tab
(number_content number(10) primary key,
 var_content varchar2(1000),
 date_content date,
 content_data clob);
#drop table clob_tab;

create table blob_tab
(number_content number(10) primary key,
 var_content varchar2(1000),
 date_content date,
 content_data blob);

#drop table blob_tabte 
;
